backend:
  name: git-gateway
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  - name: "blog"
    label: "Yazılar / Makaleler"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Başlık", name: "title", widget: "string"}
      - {label: "Tarih", name: "date", widget: "datetime"}
      - {label: "Etiket", name: "tag", widget: "select", options: ["Ceza Hukuku", "Gayrimenkul", "İş Hukuku", "Aile Hukuku", "Veraset", "Şirketler Hukuku", "Genel"]}
      - {label: "Makale Özeti", name: "description", widget: "text", hint: "Kartlarda görünecek kısa özeti buraya yazın."}
      - {label: "İçerik", name: "body", widget: "markdown"}